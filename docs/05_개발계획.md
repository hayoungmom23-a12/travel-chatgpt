# 여행 조건 기반 일자별 여행 계획 도구 개발 계획

## EPIC 1. 여행 계획 생성·편집·복구 완성

| EPIC | STORY ID | 작업 | 기능 ID | 수정할 파일 | 완료 조건 |
| --- | --- | --- | --- | --- | --- |
| EPIC 1 | E1-S01 | 여행지 입력·지우기 UI를 구현한다. | F-01 | `package.json`, `app/layout.tsx`, `app/page.tsx`, `components/planner/PlannerShell.tsx`, `components/planner/TripForm.tsx`, `lib/schemas/plan.ts` | 도시·지역명 입력과 지우기가 입력 화면에 반영된다. |
| EPIC 1 | E1-S02 | 추천 여행지 칩으로 여행지 값을 교체한다. | F-02 | `components/planner/TripForm.tsx` | 칩 선택 시 여행지 입력값이 선택한 지역명으로 바뀐다. |
| EPIC 1 | E1-S03 | 출발일·도착일 입력과 여행 일수·박수 계산을 구현한다. | F-03 | `components/planner/TripForm.tsx`, `lib/schemas/plan.ts`, `lib/plan/dates.ts` | 올바른 날짜 범위에서 N박 N일이 표시되고, 역순 날짜는 입력 오류로 표시된다. |
| EPIC 1 | E1-S04 | 인원과 동행자 관계 선택을 구현한다. | F-04 | `components/planner/TripForm.tsx`, `lib/schemas/plan.ts` | 1명·혼자 관계와 2명 이상·동행 관계의 조합 규칙이 화면에 반영된다. |
| EPIC 1 | E1-S05 | 총예산 입력과 1인 참고 금액 계산을 구현한다. | F-05 | `components/planner/TripForm.tsx`, `lib/schemas/plan.ts`, `lib/plan/budget.ts` | 양의 원화 금액이 저장되고 인원에 맞는 1인 참고 금액 또는 범위가 표시된다. |
| EPIC 1 | E1-S06 | 여행 스타일 다중 선택을 구현한다. | F-06 | `components/planner/TripForm.tsx`, `lib/schemas/plan.ts` | 스타일을 여러 개 선택·해제할 수 있고 중복값은 저장되지 않는다. |
| EPIC 1 | E1-S07 | 피하고 싶은 것을 태그로 추가·삭제한다. | F-07 | `components/planner/TripForm.tsx`, `lib/schemas/plan.ts` | 자유 입력값이 태그로 추가되고 빈 값·중복 태그는 추가되지 않는다. |
| EPIC 1 | E1-S08 | 생성 요청 전 입력값을 검증하고 오류 항목으로 이동한다. | F-08 | `components/planner/TripForm.tsx`, `components/planner/PlannerShell.tsx`, `lib/schemas/plan.ts` | 필수값 또는 형식 오류가 있으면 첫 오류 입력란과 메시지가 표시되고 요청하지 않는다. |
| EPIC 1 | E1-S09 | 계획 생성 요청과 처리 중 단계를 연결한다. | F-09 | `app/api/plans/route.ts`, `components/planner/PlanningProgress.tsx`, `components/planner/PlannerShell.tsx`, `lib/ai/generate-plan.ts`, `lib/ai/prompt.ts`, `lib/plan/validate-result.ts`, `lib/errors.ts` | 유효한 요청 후 처리 단계가 보이고, 서버가 검증된 계획 JSON 또는 오류 상태를 반환한다. |
| EPIC 1 | E1-S10 | 생성 계획의 여행 요약과 이동 피로도 참고 지표를 표시한다. | F-10 | `components/planner/ResultSummary.tsx`, `lib/schemas/plan.ts` | 여행지·기간·인원·관계·총예산·1인 참고 금액·이동 피로도가 결과 상단에 표시된다. |
| EPIC 1 | E1-S11 | 선택한 일자의 시간대별 코스를 표시한다. | F-11 | `components/planner/DayCourseEditor.tsx`, `lib/schemas/plan.ts` | 일자 탭을 바꾸면 해당 날짜의 활동·식사·이동·휴식 코스가 표시된다. |
| EPIC 1 | E1-S12 | 항목별 예상 예산표를 표시한다. | F-12 | `components/planner/BudgetEditor.tsx`, `lib/plan/budget.ts`, `lib/schemas/plan.ts` | 총액·1인 환산액·잔여 예산과 항목별 추정 금액이 표시된다. |
| EPIC 1 | E1-S13 | 금액·운영 정보의 추정치 및 공식 채널 확인 안내를 표시한다. | F-15 | `components/planner/NoticePanel.tsx`, `lib/plan/validate-result.ts` | 결과 화면에 `추정치`와 `공식 채널 확인 필요` 안내가 표시된다. |
| EPIC 1 | E1-S14 | 준비물 체크리스트를 표시하고 완료 상태를 토글한다. | F-13 | `components/planner/ChecklistEditor.tsx`, `components/planner/PlannerShell.tsx`, `lib/schemas/plan.ts` | 준비물 목록이 표시되고 체크·해제 상태가 즉시 바뀐다. |
| EPIC 1 | E1-S15 | 활동별 우천 시 실내 대안을 표시한다. | F-14 | `components/planner/RainyAlternativeEditor.tsx`, `lib/schemas/plan.ts` | 기존 야외 활동과 대응하는 우천 대안이 결과에 표시된다. |
| EPIC 1 | E1-S16 | 일자별 코스 항목을 직접 편집하고 저장·취소한다. | F-16 | `components/planner/DayCourseEditor.tsx`, `components/planner/PlannerShell.tsx`, `lib/schemas/plan.ts` | 활동명·설명·시간을 편집할 수 있고 시간 순서 오류는 저장되지 않는다. |
| EPIC 1 | E1-S17 | 예산 항목과 금액을 직접 편집하고 합계를 재계산한다. | F-16 | `components/planner/BudgetEditor.tsx`, `lib/plan/budget.ts`, `lib/schemas/plan.ts` | 유효한 금액을 저장하면 합계·1인 환산액·잔여 예산이 즉시 갱신되고 사용자 수정 상태가 표시된다. |
| EPIC 1 | E1-S18 | 준비물 항목을 직접 편집한다. | F-16 | `components/planner/ChecklistEditor.tsx`, `lib/schemas/plan.ts` | 준비물을 추가·수정·삭제할 수 있고 수정 항목이 사용자 수정으로 구분된다. |
| EPIC 1 | E1-S19 | 우천 시 대안을 직접 편집한다. | F-16 | `components/planner/RainyAlternativeEditor.tsx`, `lib/schemas/plan.ts` | 대안명·설명을 수정할 수 있고 빈 대안명은 저장되지 않는다. |
| EPIC 1 | E1-S20 | 기존 조건을 다시 불러와 전체 계획을 새로 생성한다. | F-16 | `components/planner/ResultActions.tsx`, `components/planner/PlannerShell.tsx`, `components/planner/TripForm.tsx` | `조건 다시 수정하기`를 누르면 기존 조건이 폼에 채워지고, 새 생성 후 결과가 교체된다. |
| EPIC 1 | E1-S21 | 장소명을 외부 지도 검색 화면으로 연다. | F-17 | `components/planner/ResultActions.tsx`, `lib/maps/external-search.ts` | 선택 장소의 인코딩된 검색어로 허용된 외부 지도 서비스가 새 탭에서 열린다. |
| EPIC 1 | E1-S22 | 현재 기기·브라우저에 일정을 보관하고 다시 연다. | F-18 | `components/planner/ResultActions.tsx`, `lib/storage/saved-plans.ts`, `components/planner/PlannerShell.tsx` | 로그인 없이 보관한 계획과 체크·직접 편집 상태를 같은 브라우저에서 복원할 수 있다. |
| EPIC 1 | E1-S23 | 생성 결과를 시스템 공유 또는 클립보드로 전달한다. | F-19 | `components/planner/ResultActions.tsx`, `lib/share/plan-share.ts` | 공유 창이 열리거나 지원하지 않는 환경에서는 공유 텍스트가 복사 또는 표시된다. |
| EPIC 1 | E1-S24 | 조건 충돌을 API 응답으로 구분한다. | F-20 | `app/api/plans/route.ts`, `lib/ai/generate-plan.ts`, `lib/errors.ts`, `lib/schemas/plan.ts` | 계획 생성이 어려운 조건은 `CONDITION_CONFLICT`와 조정안 데이터로 반환된다. |
| EPIC 1 | E1-S25 | 조건 충돌 이유와 조정안을 오류 화면에 표시한다. | F-20 | `components/planner/ConflictPanel.tsx`, `components/planner/PlannerShell.tsx` | 예산·기간 등 충돌 이유와 예산 조정·일정 축소 제안이 브라우저에 표시된다. |
| EPIC 1 | E1-S26 | 선택한 자동 조정안을 적용해 다시 생성한다. | F-20 | `components/planner/ConflictPanel.tsx`, `components/planner/PlannerShell.tsx`, `components/planner/TripForm.tsx` | 사용자가 선택한 조정값만 적용한 뒤 처리 중 화면을 거쳐 새 계획을 생성한다. |
| EPIC 1 | E1-S27 | 생성 실패 원인과 복구 행동을 오류 화면에 표시한다. | F-21 | `components/planner/ErrorPanel.tsx`, `components/planner/PlannerShell.tsx`, `lib/errors.ts` | 응답 지연·생성 오류·불완전 결과가 내부 오류 없이 사용자용 안내와 함께 표시된다. |
| EPIC 1 | E1-S28 | 실패한 생성 요청을 같은 조건으로 재시도한다. | F-21 | `components/planner/ErrorPanel.tsx`, `components/planner/PlannerShell.tsx` | 재시도 시 마지막 유효 입력값을 유지한 채 처리 중 화면으로 돌아가며, 조건 수정도 선택할 수 있다. |

EPIC 1 완료 기준: 유효한 여행 조건 입력부터 생성·직접 편집·다시 만들기·지도 확인·기기 보관·공유·조건 충돌·생성 실패 복구까지 전 기능을 로컬 브라우저에서 확인할 수 있으며, 이 완성 상태를 단일 되돌림 기준점으로 삼는다.
